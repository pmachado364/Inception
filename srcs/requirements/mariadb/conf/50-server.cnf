# MariaDB server configuration for Docker

[mysqld]

#
# * Basic Settings
#

bind-address = 0.0.0.0
port = 3306
datadir = /var/lib/mysql
character-set-server = utf8mb4
collation-server = utf8mb4_general_ci